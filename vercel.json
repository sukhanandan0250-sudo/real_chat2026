{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "if [ -f frontend/package.json ]; then npm install --prefix frontend && npm install --prefix backend; else npm install && npm install --prefix ../frontend; fi",
  "buildCommand": "if [ -f frontend/package.json ]; then npm --prefix frontend run build; else npm --prefix ../frontend run build && mkdir -p frontend && cp -R ../frontend/build frontend/build; fi",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/user/(.*)",
      "destination": "/api/user/$1"
    },
    {
      "source": "/message/(.*)",
      "destination": "/api/message/$1"
    },
    {
      "source": "/uploads/(.*)",
      "destination": "/api/uploads/$1"
    },
    {
      "source": "/socket.io/(.*)",
      "destination": "/api/socket.io/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
